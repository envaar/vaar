# Basic Example

The given `.env.example` is an example of a clean file with no linting issues.

## Contents

- `.env.example` is safe to commit.
- The (dummy) values are insignificant from a security standpoint.

## What This Example Shows

- valid key naming
- standard `=` delimiters
- values without extra whitespace
- a final newline
- no BOM
- no mixed line endings
